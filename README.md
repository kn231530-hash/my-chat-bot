# My AI Chatbot

A small Next.js AI chatbot, ready for Vercel.

## Run locally

1. Install: `npm install`
2. Create `.env.local`
3. Add: `OPENAI_API_KEY=your_key_here`
4. Run: `npm run dev`

Never commit your API key to GitHub.